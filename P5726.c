//#include<stdio.h>
//int main()
//{
//	int n, num, i;
//	scanf("%d", &n);
//	scanf("%d", &num);
//	double sum = num;
//	int max = num, min = num;
//	for (i = 2; i <= n; i++)
//	{
//		scanf("%d", &num);
//		sum += num;
//		max = max > num ? max : num;
//		min = min < num ? min : num;
//	}
//	sum -= min + max;
//	printf("%.2lf", sum / (n - 2));
//	return 0;
//}